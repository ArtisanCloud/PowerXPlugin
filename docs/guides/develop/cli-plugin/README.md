# CLI Plugin 指南

本目录用于统一维护 `px-plugin` 相关的开发文档。

## 文档列表

- `cli-plugin-tutorial.md`：使用 CLI 初始化、启动并本地安装插件的完整教程。
- `init-fastapi.md`：使用 `px-plugin init` 生成 Python FastAPI 后端插件项目。
- `init-next-react.md`：使用 `px-plugin init` 生成 Next（React）管理端插件项目。
