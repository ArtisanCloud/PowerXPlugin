# Standalone 启动（NextJS 前端）

本页仅覆盖 NextJS Web Admin 的 Skeleton 启动流程。

## 目录

- `skeleton/web-admin/next`

## 快速启动

```bash
cd skeleton/web-admin/next
npm install
npm run dev
```

## 说明

- 默认监听 `http://localhost:3231`。
- 当前 NextJS 为最小空壳，仅用于技术栈接入与组合验证。
