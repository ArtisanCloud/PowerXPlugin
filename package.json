{
  "name": "powerxplugin-monorepo",
  "private": true,
  "workspaces": [
    "framework/frontend/nuxt/framework-admin",
    "framework/frontend/nuxt/framework-client"
  ],
  "scripts": {
    "lint": "npm --workspaces --if-present run lint && npm --prefix skeleton/web-admin run lint",
    "build": "npm --workspaces --if-present run build && npm --prefix skeleton/web-admin run build",
    "sync:templates": "node scripts/sync-templates.mjs",
    "test": "node scripts/capabilities/run-from-package.mjs"
  },
  "dependencies": {
    "fast-glob": "^3.3.3",
    "micromatch": "^4.0.8",
    "yaml": "^2.8.1"
  }
}
