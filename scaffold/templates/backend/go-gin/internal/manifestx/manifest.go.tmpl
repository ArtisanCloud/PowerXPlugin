package manifestx

import "github.com/powerx-plugin/framework/backend/go/manifest"

// Plugin 返回插件的默认 Manifest 定义。
func Plugin() manifest.Plugin {
	return manifest.Plugin{
		ID:      "{{ .PluginID }}",
		Name:    "{{ .PluginName }}",
		Version: "{{ .Version }}",
		Menus: []manifest.Menu{
			{
				Path:  "/_p/{{ .PluginID }}/admin",
				Title: "{{ .PluginName }}",
			},
		},
		Permissions: []string{
			"{{ .PluginID }}.admin.view",
		},
	}
}
