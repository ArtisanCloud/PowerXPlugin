version: v1
topics:
  - key: _topic.template.update
    namespace: _topic.template
    name: update
    description: 模板状态更新事件
    acl:
      - principal_type: member
        principal_id: "member:system"
        actions: [publish, subscribe]
      - principal_type: plugin
        principal_id: "plugin:{{ .PluginID }}"
        actions: [publish]

  - key: _topic.audit.template.updated
    namespace: _topic.audit.template
    name: updated
    description: 模板审计更新事件
    acl:
      - principal_type: member
        principal_id: "member:system"
        actions: [publish]
      - principal_type: plugin
        principal_id: "plugin:{{ .PluginID }}"
        actions: [publish]

  - key: _topic.template.validate.completed
    namespace: _topic.template.validate
    name: completed
    description: 模板校验完成事件
    acl:
      - principal_type: member
        principal_id: "member:system"
        actions: [publish]
      - principal_type: plugin
        principal_id: "plugin:{{ .PluginID }}"
        actions: [publish]

  - key: _topic.template.batch_clone.completed
    namespace: _topic.template.batch_clone
    name: completed
    description: 模板批量克隆完成事件
    acl:
      - principal_type: member
        principal_id: "member:system"
        actions: [publish]
      - principal_type: plugin
        principal_id: "plugin:{{ .PluginID }}"
        actions: [publish]

  - key: _topic.template.update.completed
    namespace: _topic.template.update
    name: completed
    description: 模板更新完成事件
    acl:
      - principal_type: member
        principal_id: "member:system"
        actions: [publish]
      - principal_type: plugin
        principal_id: "plugin:{{ .PluginID }}"
        actions: [publish]

  - key: powerx.runtime.scheduler.triggered.v1
    namespace: powerx.runtime.scheduler.triggered
    name: v1
    description: Scheduler 标准触发事件
    acl:
      - principal_type: member
        principal_id: "member:system"
        actions: [publish, subscribe]
      - principal_type: plugin
        principal_id: "plugin:{{ .PluginID }}"
        actions: [publish]

  - key: _topic.notify.tenant.{{tenant_uuid}}
    namespace: _topic.notify.tenant
    name: "{{tenant_uuid}}"
    description: 租户通知事件
    acl:
      - principal_type: member
        principal_id: "member:system"
        actions: [publish, subscribe]
      - principal_type: plugin
        principal_id: "plugin:{{ .PluginID }}"
        actions: [publish]

  - key: _topic.notify.member.{{member_uuid}}
    namespace: _topic.notify.member
    name: "{{member_uuid}}"
    description: 成员通知事件
    acl:
      - principal_type: member
        principal_id: "member:system"
        actions: [publish, subscribe]
      - principal_type: plugin
        principal_id: "plugin:{{ .PluginID }}"
        actions: [publish]

  - key: _topic.iam.wecom.sync.progress
    namespace: _topic.iam.wecom.sync
    name: progress
    description: 企业微信同步进度事件
    acl:
      - principal_type: member
        principal_id: "member:system"
        actions: [publish, subscribe]
      - principal_type: plugin
        principal_id: "plugin:{{ .PluginID }}"
        actions: [publish]

  - key: _topic.iam.dingtalk.sync.progress
    namespace: _topic.iam.dingtalk.sync
    name: progress
    description: 钉钉同步进度事件
    acl:
      - principal_type: member
        principal_id: "member:system"
        actions: [publish, subscribe]
      - principal_type: plugin
        principal_id: "plugin:{{ .PluginID }}"
        actions: [publish]

  - key: _topic.iam.lark.sync.progress
    namespace: _topic.iam.lark.sync
    name: progress
    description: 飞书同步进度事件
    acl:
      - principal_type: member
        principal_id: "member:system"
        actions: [publish, subscribe]
      - principal_type: plugin
        principal_id: "plugin:{{ .PluginID }}"
        actions: [publish]
