<template>
  <UContainer class="prose dark:prose-invert max-w-3xl py-10">
    <h1>{{ "{{ $t('templates.develop.title') }}" }}</h1>
    <p>{{ "{{ $t('templates.develop.intro') }}" }}</p>

    <h2>目录结构</h2>
    <pre><code>app/
  pages/
    intro.vue
    templates/
      index.vue
      crud.vue
      develop.vue
  components/
    templates/
      TemplateFormModal.vue
    ConfirmDialog.vue
    ToastAlert.vue
  composables/
    api/
      useTemplate.ts
</code></pre>

    <h2>数据流</h2>
    <ul>
      <li>组件通过 <code>useTemplateApi</code> 调用后端 API。</li>
      <li><code>ConfirmDialog</code> 与 <code>ToastAlert</code> 统一处理交互反馈。</li>
      <li>租户 ID 默认通过中间件注入，无需额外处理。</li>
    </ul>

    <h2>扩展建议</h2>
    <ul>
      <li>接入权限控制，结合 framework-admin 指令。</li>
      <li>为列表增加分页、筛选参数并传递给 API。</li>
      <li>使用 Playwright 编写端到端测试，保障 CRUD 流程。</li>
    </ul>
  </UContainer>
</template>

<script setup lang="ts">
definePageMeta({
  title: 'TemplatesDevelopGuide'
})
</script>
