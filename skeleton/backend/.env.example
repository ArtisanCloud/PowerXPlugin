# 运行模式组合速查（按需二选一启用）
# 1) standalone_local（本地 IAM + 本地链路，默认）
IAMMode=local
POWERX_PROXY=0

# 2) local + proxy（本地 IAM + 宿主链路，调试态）
# IAMMode=local
# POWERX_PROXY=1
# POWERX_PLUGIN_REGISTRATION_MODE=local

# 3) standalone_mock_delegated（委派语义 + 本地链路）
# IAMMode=delegated
# POWERX_PROXY=0

# 4) host_delegated（宿主标准：委派语义 + 宿主链路）
# IAMMode=delegated
# POWERX_PROXY=1
# POWERX_PLUGIN_REGISTRATION_MODE=installed


PLUGIN_IAM_TENANT_KEY=00000000-0000-0000-0000-000000000001
PLUGIN_IAM_TENANT_NAME="Local Tenant"
PLUGIN_IAM_ADMIN_EMAIL=admin@local.test
PLUGIN_IAM_ADMIN_PASSWORD='S3cret!!'
NUXT_PUBLIC_BRIDGE_DEBUG=false

# Gateway 能力调用（供 Capability Lab / 前端调试使用）
# 默认直连 PowerX Dev API（可按需改为其它环境），API Key 或 STS client 请替换为自己账户，
# 若暂时没有凭证，可把 PX_USE_MOCK=media 之类的值写入以便返回 Mock 数据。
PX_GATEWAY_BASE_URL=http://127.0.0.1:8077
PX_GATEWAY_API_PREFIX=/api/v1
# Host/宿主模式：bearer；Standalone+Proxy：apikey
PX_GATEWAY_AUTH_SCHEME=apikey
PX_GATEWAY_API_KEY=
POWERX_STS_CLIENT_ID=
POWERX_STS_CLIENT_SECRET=
PX_GATEWAY_TIMEOUT=60s
# PX_USE_MOCK=

# Knowledge provider：
# - local：插件内存/本地调试数据，不读取 PowerX 底座知识空间
# - delegated：通过 Gateway 委托 PowerX 底座知识库；host_delegated 模式默认 delegated
# POWERX_KNOWLEDGE_MODE=delegated
# POWERX_KNOWLEDGE_DELEGATE_TIMEOUT=3s

# 模拟宿主/委派 IAM（按需启用）
# POWERX_PROXY=1
# POWERX_CORE_ENDPOINT=http://localhost:8090
# POWERX_SECURITY_JWT_ISSUER=powerx-auth
# POWERX_SECURITY_JWT_AUDIENCE=plugin:com.powerx.plugins.base
