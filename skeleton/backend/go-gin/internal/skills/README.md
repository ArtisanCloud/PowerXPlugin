# Skeleton Skills

This directory contains minimal Agent Skill Bridge examples for the skeleton plugin.

The sample registers `mediax.video_rebuilder.cn`, exposes it through `/api/v1/plugin/skills`, and returns a queued task from the unified executor path.
