# Backend (Python FastAPI)

This is a placeholder skeleton for the Python FastAPI backend.

## Quick start

```bash
python -m venv .venv
. .venv/bin/activate
python -m pip install -U pip
pip install -r requirements.txt
bash scripts/dev.sh
```

Default health check: `http://127.0.0.1:8277/healthz`.
