<template>
  <UContainer class="py-10 space-y-6">
    <div class="space-y-2">
      <h1 class="text-2xl font-semibold text-gray-900 dark:text-white">
        {{ "{{ $t('templates.overview.title') }}" }}
      </h1>
      <p class="text-gray-600 dark:text-gray-300">
        {{ "{{ $t('templates.overview.description') }}" }}
      </p>
    </div>

    <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
      <UCard v-for="card in cards" :key="card.title">
        <template #header>
          <div class="flex items-center gap-2">
            <UIcon :name="card.icon" class="text-primary" />
            <span class="font-medium">{{ "{{ card.title }}" }}</span>
          </div>
        </template>
        <p class="text-sm text-gray-600 dark:text-gray-300">
          {{ "{{ card.description }}" }}
        </p>
      </UCard>
    </div>
  </UContainer>
</template>

<script setup lang="ts">
const cards = [
  {
    title: $t('templates.overview.cards.create'),
    description: $t('templates.overview.cards.createDescription'),
    icon: 'i-heroicons-document-plus'
  },
  {
    title: $t('templates.overview.cards.automation'),
    description: $t('templates.overview.cards.automationDescription'),
    icon: 'i-heroicons-rocket-launch'
  },
  {
    title: $t('templates.overview.cards.analytics'),
    description: $t('templates.overview.cards.analyticsDescription'),
    icon: 'i-heroicons-chart-bar'
  }
]

definePageMeta({
  title: 'TemplatesOverview'
})
</script>
